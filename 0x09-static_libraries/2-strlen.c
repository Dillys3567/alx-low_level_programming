#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - string length
 * @s: string pointer
 * Return: length
 */
int _strlen(char *s)
{
	return (strlen(s));
}

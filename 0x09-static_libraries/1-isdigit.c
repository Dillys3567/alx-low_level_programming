#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - check for digit
 * @c: digit
 * Return: 0 or 1
 */
int _isdigit(int c)
{
	return (isdigit(c));
}

#include <stdio.h>
/**
 * main - print characters with putchar
 * Return: Returns 0
 */
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
